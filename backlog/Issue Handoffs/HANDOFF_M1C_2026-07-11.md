# HANDOFF — M1-C (dependency documentation, #18) — 2026-07-11

**Branch:** `phase3_revisions`  **Base HEAD:** `27459ff` ("cleanup sql")
**Scope:** Milestone 1, M1-C (#18) — document every external resource (purpose,
license, self-containment) toward a license-compliant, air-gap-capable on-prem
release. Feeds M1-H (#36).

**Deliverables** (`/mnt/user-data/outputs/`):
- `DEPENDENCIES.md` — the dependency inventory (also the readable copy).
- `m1c_dependencies_doc.patch` — `git apply`-compatible patch that adds
  `DEPENDENCIES.md` to the repo root (+119, new file).

Re-baselined from a fresh clone; confirmed HEAD `27459ff` with M1-B committed
(`2f5ca53`, `15c00ae`). This is a **documentation-only** deliverable — no code,
schema, or config changes. Every fact was read from the tree, not recalled.

## What the doc covers

Each resource is tagged **redistributed** (vendored), **user-provided**
(host prerequisite), or **requires internet**.

- **Runtime prerequisites (user-provided):** Apache 2.4 (Apache-2.0), PHP 8.3
  (PHP-3.01), MySQL 8.0/8.4 **or** MariaDB 10.11+ (GPLv2, per M1-G), and an
  optional user-provided SMTP relay. Required PHP extensions: `mysqli`,
  `mbstring`, `openssl` (email only); `zlib` optional (FPDF compression).
- **Bundled libraries (redistributed, no Composer/npm):**
  - **FPDF 1.9** (`php/fpdf.php`) — PDF cage cards / clip sheet; FPDF permissive
    license (`php/license.txt`).
  - **PHPMailer 6.9.3** (`includes/vendor/PHPMailer/`) — SMTP mail; **LGPL-2.1**
    (`LICENSE` bundled); included via plain `require_once` in `includes/mail.php`.
  - **Tabler Icons 3.33.0** (`assets/tabler/`) — UI icon font; **MIT**.
- **First-party:** `mousebook.css` / `mousebook.js` (MIT). UI is **vanilla CSS/JS**
  — no framework, no CDN-loaded libraries.

## Findings worth flagging

1. **Tabler CDN fallback (the one internet nuance).** `mousebook.js` L31 defines a
   jsDelivr CDN for Tabler, but only as a **fallback** when the vendored
   `assets/tabler/` copy is absent (degrading to text labels if both fail). With
   the local copy present — the normal deploy — **no internet is used**. Documented
   as such; the air-gap section confirms the only runtime egress is DB + optional
   SMTP.
2. **No Composer / no npm.** All third-party PHP is vendored and manually required;
   there is no lockfile or build step. Verified (`composer.json`/`autoload.php`
   absent).
3. **README is stale** (cites "PHP 5.4 / MySQL 5.6" and the pre-`config.php`
   search-replace install). Left untouched here — refreshing it is **M1-H**, which
   should link `DEPENDENCIES.md`. Called out in the doc's compliance checklist.
4. **License compatibility:** MIT project + MIT (Tabler) + LGPL-2.1 (PHPMailer,
   used unmodified) + FPDF-permissive — all compatible with MIT redistribution.

## Validation

- Versions/licenses read directly from source: FPDF `FPDF_VERSION`/`license.txt`;
  PHPMailer `const VERSION`/`LICENSE` header; Tabler `3.33.0` in the min.css.
- Extension usage counted from the tree (`mysqli` 23 files; `mbstring`; `openssl`
  in `mail.php`; `zlib` via FPDF `function_exists('gzcompress')`).
- `git apply --check` clean for `m1c_dependencies_doc.patch` on a fresh
  `phase3_revisions` clone at `27459ff`.

## Deploy sequence

Doc-only. `git apply m1c_dependencies_doc.patch` (or drop `DEPENDENCIES.md` at the
repo root), review, commit. Nothing to run.

## Housekeeping note (carry-forward, not addressed here)

The two M1-B patch files (`m1b_allele_line_guards.patch`,
`m1b_dob_enforcement.patch`) are currently committed at the **repo root** — the
same kind of dangling-artifact clutter T0.5 cleaned up. Recommend relocating to
`backlog/` (or removing) in a future housekeeping pass.

## Next in Milestone 1 (per SCOPE rev.3)

- **M1-D (#26)** — reactive UI / remove manual REFRESH.
- **M1-E (#28)** — admin genotype-backfill repair surface (consumes
  `AUDIT_M1B_allele_line_sideeffects.md`).
- **M1-F (#27)** — concurrency behavior documentation.
- **M1-G (#37)** — DB-engine portability (MariaDB harness).
- **M1-H (#36)** — install/release readiness — **depends on M1-C (this) + M1-G**;
  should fold `DEPENDENCIES.md` into a refreshed README and deploy docs.
